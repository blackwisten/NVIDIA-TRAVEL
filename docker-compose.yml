version: '3.8'
service:
  app:
    build: .
    container_name: nvidia_travel
    ports:
      - "7860:7860"
    env_file:
      - .env

    volumes:
      - ./dataset:/app/dataset
    restart: unless-stopped
    working_dir: /app
